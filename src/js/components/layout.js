import 'bootstrap/dist/css/bootstrap.min.css';import 'bootstrap-icons/font/bootstrap-icons.css';import '../../css/styles.css';import{currentUser,signOut}from'../services/auth.js';import{isDemoMode}from'../config.js';
const links=[['index.html','Home'],['focus.html','Focus'],['music.html','Music'],['activities.html','Move'],['herbs.html','Herbs'],['journal.html','Journal']];
export async function mountLayout(active=''){const user=await currentUser();document.querySelector('#app-nav').innerHTML=`<nav class="navbar navbar-expand-lg sticky-top"><div class="container"><a class="navbar-brand" href="index.html"><span class="brand-mark"><i class="bi bi-flower1"></i></span>WellSpring</a><button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navMenu"><span class="navbar-toggler-icon"></span></button><div class="collapse navbar-collapse" id="navMenu"><ul class="navbar-nav mx-auto">${links.map(([href,label])=>`<li class="nav-item"><a class="nav-link ${active===label?'active':''}" href="${href}">${label}</a></li>`).join('')}</ul><div class="d-flex align-items-center gap-2">${isDemoMode?'<span class="pill d-none d-lg-inline-flex"><i class="bi bi-wifi-off"></i> Demo mode</span>':''}${user?`<a class="btn btn-outline-primary btn-sm" href="profile.html"><i class="bi bi-person"></i> ${user.user_metadata?.full_name?.split(' ')[0]||'Profile'}</a><button id="logout" class="btn btn-primary btn-sm">Log out</button>`:'<a class="btn btn-primary btn-sm" href="auth.html">Sign in</a>'}</div></div></div></nav>`;document.querySelector('#app-footer').innerHTML=`<footer><div class="container d-flex flex-column flex-md-row justify-content-between gap-2"><div><strong>WellSpring</strong> · small steps, steadier days</div><div>Educational wellness support — not medical advice.</div></div></footer>`;if(user)document.querySelector('#logout')?.addEventListener('click',async()=>{await signOut();location.href='index.html'});import('bootstrap')}
export function toast(message,type='success'){const box=document.querySelector('#toast-area')||document.body.appendChild(Object.assign(document.createElement('div'),{id:'toast-area',className:'toast-container position-fixed bottom-0 end-0 p-3'}));const el=document.createElement('div');el.className=`toast show text-bg-${type} border-0`;el.innerHTML=`<div class="d-flex"><div class="toast-body">${message}</div><button class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button></div>`;box.appendChild(el);setTimeout(()=>el.remove(),3500)}
