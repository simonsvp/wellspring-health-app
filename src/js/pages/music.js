import{mountLayout,toast}from'../components/layout.js';import{list}from'../services/data.js';mountLayout('Music');const tracks=await list('tracks');document.querySelector('#tracks').innerHTML=tracks.map(t=>`<div class="col-md-6"><article class="soft-card audio-card"><div class="audio-visual mb-3">${[35,60,85,48,72,40,65,30].map(h=>`<span style="--h:${h}%"></span>`).join('')}</div><div class="d-flex align-items-center justify-content-between"><div><span class="pill">${t.kind}</span><h3 class="h5 mt-2 mb-1">${t.title}</h3><small class="text-secondary">${t.minutes} min</small></div><button class="btn btn-primary play" aria-label="Play ${t.title}"><i class="bi bi-play-fill"></i></button></div></article></div>`).join('');document.querySelectorAll('.play').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.audio-card').forEach(x=>x.classList.remove('playing'));b.closest('.audio-card').classList.add('playing');b.innerHTML='<i class="bi bi-pause-fill"></i>';toast('Preview started. Connect Supabase Storage audio for production.','dark')}));
