import{mountLayout,toast}from'../components/layout.js';import{list,save,remove}from'../services/data.js';mountLayout('Journal');let mood='🙂';const box=document.querySelector('#entries');document.querySelectorAll('.mood-button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.mood-button').forEach(x=>x.classList.remove('active'));b.classList.add('active');mood=b.dataset.mood});async function draw(){const rows=await list('journal_entries');box.innerHTML=rows.length?rows.map(x=>`<article class="journal-entry mb-4"><div class="d-flex justify-content-between"><div><span class="fs-4">${x.mood}</span><strong class="ms-2">${new Date(x.created_at).toLocaleDateString()}</strong></div><button class="btn btn-sm text-danger delete" data-id="${x.id}"><i class="bi bi-trash"></i></button></div><p class="mt-2 mb-0">${x.content}</p></article>`).join(''):'<div class="empty"><i class="bi bi-journal-heart fs-1"></i><p>Your reflections will appear here.</p></div>';document.querySelectorAll('.delete').forEach(b=>b.onclick=async()=>{await remove('journal_entries',b.dataset.id);draw()})}document.querySelector('#journal-form').onsubmit=async e=>{e.preventDefault();const content=document.querySelector('#reflection').value.trim();if(!content)return;await save('journal_entries',{mood,content});e.target.reset();toast('Reflection saved');draw()};draw();
