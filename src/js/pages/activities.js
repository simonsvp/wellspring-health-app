import{mountLayout,toast}from'../components/layout.js';import{list,save}from'../services/data.js';mountLayout('Move');const rows=await list('activities'),grid=document.querySelector('#activity-grid');function draw(filter='All'){grid.innerHTML=rows.filter(x=>filter==='All'||x.category===filter).map(x=>`<div class="col-md-6 col-xl-4"><article class="soft-card"><div class="icon-tile"><i class="bi ${x.icon}"></i></div><span class="pill mt-3">${x.category}</span><h3 class="h4 mt-2">${x.title}</h3><p class="text-secondary">${x.minutes} minutes · ${x.level}</p><button class="btn btn-outline-primary complete" data-id="${x.id}"><i class="bi bi-check2"></i> Mark complete</button></article></div>`).join('');document.querySelectorAll('.complete').forEach(b=>b.onclick=async()=>{const x=rows.find(r=>String(r.id)===b.dataset.id);await save('activity_logs',{activity_id:x.id,duration_minutes:x.minutes});b.innerHTML='<i class="bi bi-check-circle-fill"></i> Completed';b.disabled=true;toast('Activity added to your day')})}draw();document.querySelectorAll('.filter-chip').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter-chip').forEach(x=>x.classList.remove('active'));b.classList.add('active');draw(b.dataset.filter)});
